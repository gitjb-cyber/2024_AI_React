import { CSSProperties } from "react";

type DagymProps = {
  columnNumber: number;
};

const DagymMain = (props: DagymProps) => {
  const DagymMainStyle: CSSProperties = {};

  return <div style={DagymMainStyle}></div>;
};

export default DagymMain;
