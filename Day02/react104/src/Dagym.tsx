import { CSSProperties } from "react";
import data from "./data";
import { maxHeaderSize } from "http";

type DagymProps = {
  liteAdListNum: number;
  gymName: string;
  address: string;
  tags: [1, 2, 3, 4, 5];
  program: string;
  service: string;
  price: number;
};

const Dagym = () => {
      const main = {
        width: "100vw",
      };
      const container = {
        width: "100px",
        margin:
      };  
    const DagymStyle: CSSProperties = {

      };



  return <div style={DagymStyle}></div>;
};

export default Dagym;
